# VITE_API_URL is the deployed Render backend URL.
# Leave empty for local development (uses the Vite proxy instead).
# Example for production:
# VITE_API_URL=https://farmdirect-backend.onrender.com
VITE_API_URL=
